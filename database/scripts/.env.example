# Used by migrate_data_js.js. Copy this to .env in this folder and fill in
# the same backend-only Supabase values as backend/.env — never commit
# real values.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
